export type PublishedApplets = { recordsPerPage: number; pageIndex: number; searchText: string };

export type PublishedApplet = { libraryId: string };

export type AppletContent = { libraryId: string; appletId?: string };
